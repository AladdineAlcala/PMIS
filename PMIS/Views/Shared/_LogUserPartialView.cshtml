@using Microsoft.AspNet.Identity
@model PMIS.ViewModels.UsersViewModel


@if (Request.IsAuthenticated)
{
    <!-- User Dropdown Menu -->

    <li class="nav-item dropdown user-menu">
        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
            <img src="@Url.Content("~/Content/dist/img/user2-160x160.jpg")" class="user-image img-circle elevation-2" alt="User Image">
            <span class="d-none d-md-inline">@Model.username</span>
        </a>
        <ul class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
            <!-- User image -->
            <li class="user-header bg-primary">
                <img src="@Url.Content("~/Content/dist/img/user2-160x160.jpg")" class="img-circle elevation-2" alt="User Image">

                <p>
                    @Model.username - <span class="text text-center">@Model.roles</span>
                    @*<small>Member since Nov. 2012</small>*@
                </p>
            </li>
            <!-- Menu Body -->
            <li class="user-body">
                <div class="row">
                    
                    <div class="col-5 text-center">
                        <a href="@Url.Action("UserProfile","Account",new{userId=Model.userId})" class="btn btn-default btn-flat">Profile</a>
                    </div>
                    
                    <div class="col-2">
                        
                    </div>
                    
                    
                 
                        <div class="col-5 text-center">
                            @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new {id = "logout"}))
                            {
                                @Html.AntiForgeryToken()
                                <a href="javascript:document.getElementById('logout').submit()" class="btn btn-default btn-flat float-right">Sign out</a>
                            }
                        </div>

                    
                  
               

                </div>
                <!-- /.row -->
            </li>
          
        </ul>

    </li>
}

else
{
    <li class="nav-item d-none d-sm-inline-block">
        <a href="@Url.Action("LogIn","Account")" class="nav-link">Log-in</a>
    </li>
}


